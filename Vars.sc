val domain = <jira cloud domain>
val user = <jira user id>
val token = <jira pw or better token>

//if the story should be created only for some specific components add them to this list. By Name, not ID to make it more readable.
val componentFilter = List()
